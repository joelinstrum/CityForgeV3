# Brick Paving lot surface V01

The provided `/Users/joelinstrum/Downloads/textures/brick/brick-texture-1.png` is imported unchanged as `Assets/CityForgeV3/Resources/CityForgeV3/LotTextures/BrickPavingV01/brick-texture-1.png`. Both files have SHA-256 `c2677962aa4b6065c743426f5607103056b6774840f23d606f274f4eec3c92ee`. The Unity importer retains the source's 1254 × 1254 pixels instead of reducing a non-power-of-two image to 1024. No derived artwork was made; the versioned runtime copy preserves the supplied source.

The Lot Editor exposes **Brick Paving** under Base and Overlays. Both use the same resource and saved ID `brick-paving-v01`. Base repeats once per 10 m, matching a single 10 × 10 m overlay tile, so the brick scale stays consistent. Other base choices retain their existing 5 m repeat. The overlay uses the existing tile painting, rotation, selection, Undo and manual Save routes. It adds no pedestrian network segment; current sidewalk options retain their routing behavior. Saved lots using existing IDs need no migration.

The V01 validation image shows a temporary overlay tile beside Natural Grass in the active Lot Camera. Resource import, catalog mapping, base repeat metadata, neutral pedestrian behavior and an isolated base/overlay session JSON round trip passed. The current active Lot session JSON was unchanged; the temporary tile and Editor QA code were removed, and no Save was called. A physical UI placement pass, direct whole-lot base capture and disk save/reload remain open. The earlier forest 39/39 and regional 83/83 runs are unrelated.
