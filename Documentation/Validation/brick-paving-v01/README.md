# Brick Paving V01 Lot Camera review

`lot-camera-overlay.png` is the active Lot Camera view with one temporary 10 × 10 m Brick Paving tile beside Natural Grass. The tile used the existing shadow-receiving overlay shader, render queue and height. It was destroyed after the capture. `review.txt` records catalog, import-size, scale and session round-trip checks; the active Lot JSON was unchanged and no Save was called.
