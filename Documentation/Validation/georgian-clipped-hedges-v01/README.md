# Georgian clipped hedges V01 live check

`baseline.png` is the active Lot Camera before temporary additions. `hedges-live.png` contains a new muted-edge flower bed, clipped hedge square, and clipped hedge rectangle. `hedges-rectangle-90-live.png` shows the rectangle quarter turn. `hedges-winter-tint-live.png` shows the two hedge meshes with winter tint under the unchanged summer Lot lighting; it is not a winter environment capture. `qa.txt` records the Lot camera and isolated serialization/footprint check.

The objects existed only as temporary scene presentations. The active lot serialization was compared before and after and was unchanged; no Save was called.
