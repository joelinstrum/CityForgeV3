# Schoolyard children V01 validation — September 18, 2026

An unsaved 4 × 4 Civics lot was built in an isolated Unity 6.1 batch process from the `lot-updates` worktree. The active `small-schoolhouse-3d-v01` FBX was placed at x = -5 m and one `Group of 18th Century Children` item at x = 8 m. `schoolyard-group-flipbook-selected.png` shows the group as one selectable unit with its 8 × 8 m outline. `schoolyard-group-flipbook-frame16.png` shows a later frame of the pre-rendered loop. The figures remain around the same scale as the earlier five-sprite prototype, but all five appearances, movement, shadows, and clapping are now baked into one group image per frame.

The targeted EditMode test confirms that the catalog and atlases load, a placed group has one scene renderer and one selection-outline renderer, the outline is enabled on selection, and rotation, unsaved serialization, and undo work. Results are in `schoolyard-automata-editmode-tests.xml`. The four intermediate per-character atlases are no longer in Unity Resources; the runtime loads only the group atlases.

The isolated Regions Review project received focused updates to its Automata UI and feature-owned files and assets. Desktop control selected the separate main V3 Unity session, so the live Testy District 9 scene was not inspected or altered. These images are independent worktree renders. No lot, district, or region was saved.

September 18 follow-up: the Automata tool now selects and drags the entire 8 × 8 m group directly. A drag preserves the pointer's offset within the square and creates one undo step; clicking without moving only selects. The old two-click Move button was removed. The supplied flipbook atlases were left intact. `schoolyard-group-ground-clearance.png` is a new unsaved worktree render showing the group artwork cleared above the ground-aligned selection outline. The runtime shifts the camera-facing image along the orthographic sightline using the atlas's measured one-meter visible extent below its pivot; this keeps the image at the same screen position while preventing the ground plane and outline from covering lower pixels. The focused EditMode test passed after these changes. The new code and catalog value were applied selectively to the isolated review project, preserving its other editor changes; live Testy inspection remains open because desktop control still resolves to the separate main V3 Unity session.

Direct-selection correction: clicking the visible scene now hits its camera-facing image as well as its ground footprint, and the group can be selected from other non-painting lot-editor tools. The click switches the inspector to Automata on pointer-up, where the yellow outline and group actions are shown. The targeted EditMode test exercises an upper-image click, outline enablement, direct dragging, undo, and unsaved serialization. The correction was applied selectively to the review project; the active editor has not yet been visually inspected after recompilation.

Visibility schedule: the Automata inspector now has time-of-day and season checkboxes for the selected group. This schoolchildren clip defaults to Noon in Spring and Autumn; its atlas remains one pre-rendered group, with two serialized bitmasks on the lot placement. Existing placements without schedule fields inherit the clip defaults. The selected outline remains visible while its animation is hidden, and Select Group / Next Group can reach a hidden placement from the inspector. District-hosted groups read the district's existing calendar season through a scalar provider; they do not scan lots or district objects per frame. Two focused EditMode tests verify schedule defaults, edits, undo, round-trip serialization, hidden selection, and a district season change without saving. The isolated review project received selective code and catalog updates; live editor visual confirmation is still pending.

Review compile correction: the isolated review checkout lacks the newer `ForestSeason` property used by the first season-provider binding. The binding now reads its existing `RegionCityTile.Labor.SeasonIndex` and maps 0–3 to Summer, Autumn, Winter, and Spring. The same source change was made in the worktree. Both focused worktree tests pass with the calendar mapping; the active review editor has not yet produced a fresh compile log after the correction.

Open review remains: live playback and directional changes, night lighting, rotated district lots, placement over uneven terrain, and representative dense-district CPU, allocations, draw calls, frame-time spikes, and longer stability. The group is designed for a flat schoolyard; the flipbook cannot conform each pre-rendered foot independently to uneven terrain.
