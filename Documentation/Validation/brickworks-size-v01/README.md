# Brickworks size adjustment

Regions Review only. Doubled all model dimensions using a shared presentation scale, preserving the imported prefab. Placement preview and collision footprint are now 40 × 50 metres; the receiving offset scales to 48 metres. Tree clearing follows the rotated footprint with a 3m margin. Production timing is unchanged.

Unity compilation succeeded. Editor fixture verified actual and preview model scales, footprint, rotated containment, receiving point, shared selection and rotation. See checks.txt.
