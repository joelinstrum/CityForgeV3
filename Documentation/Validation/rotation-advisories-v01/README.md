# Advisory rotation conflicts — review only

Shared action result distinguishes success with warnings from failure. Side panel saves success even with advisory text and retains the message across panel refresh; next warning-free action clears it. Quarry rotation uses former strict placement-clearance diagnostics only as warnings (road margin exceeded actual visible footprint). Brickworks delivery/placement restrictions advisory too. Placed lots bypass overlap/water transform veto for rotation only; keep grid anchor, warn about invalid footprint. Initial placement validation unchanged. Away quarry wagon world position/headings preserved and return endpoint/navigation updated.

Unity compiled. Live test through rotation button: road in quarry footprint no longer blocks; yaw changes and is serialized to isolated save; advisory appears; removal of road plus next rotation clears warning. No main-project edits. Evidence checks.txt.
