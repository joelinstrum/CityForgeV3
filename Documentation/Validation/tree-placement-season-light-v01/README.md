# Tree placement and season-light QA

`review.txt` records the isolated runtime checks. A temporary Lot fixture was
removed and its global lighting settings restored. No user lot was saved.
Focused EditMode tests are written but pending because the Editor is in Play
mode; physical UI clicks and disk save/reload are also pending.
