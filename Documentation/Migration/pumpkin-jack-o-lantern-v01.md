# Pumpkin Jack-o'-Lantern V01

- Source archive: `/Users/joelinstrum/Downloads/buildings/props/pumpkin+jack-o'-lantern+3d+model.zip` (read-only purchased source).
- Runtime identity: `pumpkin-jack-o-lantern-v01`, displayed as `Jack-o'-Lantern` in Props → Seasonal Decorations.
- Runtime model: the supplied FBX is preserved unchanged under `CityForgeV3/Props/Seasonal/PumpkinJackOLanternV01`.
- Materials: supplied base color and normal maps are explicitly rebound at runtime; metallic, roughness, and RM sources are retained alongside them for later material refinement.
- Scale contract: normalized to 0.52 meters tall with a 0.55-by-0.55-meter placement footprint.
- Season contract: `Autumn` only. The prop is omitted from the library and cannot be newly placed outside Autumn. A placed instance remains in the saved lot and becomes hidden outside Autumn, returning non-destructively when Autumn is selected again.
- Compatibility: props with no season metadata remain available during all four seasons.
